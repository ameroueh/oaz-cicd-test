name: hello
on: 
  push:
    branches: [main]
jobs:
  hello:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/ameroueh/oaz-cicd-test:latest 
      options: --user 1001
    steps:
      - uses: actions/checkout@v2
      - run: |
              echo "Hello!"
        name: hello
